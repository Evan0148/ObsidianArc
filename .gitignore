# Build output
/bin/
/dist/

# Runtime state: the database, the generated secret key, uploads.
/data/

# Frontend
web/node_modules/
web/.vite/

# Documentation
docs/node_modules/
docs/.vitepress/dist/
docs/.vitepress/cache/

# The compiled SPA is embedded by //go:embed, but it is a build artifact.
# The marker is committed so `go build` works on a fresh clone.
/internal/web/dist/*
!/internal/web/dist/.gitkeep

.DS_Store
Thumbs.db
*.log
.claude/settings.local.json

# 代理的工作草稿和一次性审计报告，不进仓库。
# 2026-09-20 被 `git add -A` 卷进去过一次，所以写下来而不是靠记性。
.agents/
docs/SECURITY_AUDIT.md
